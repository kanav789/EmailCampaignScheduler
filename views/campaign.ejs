<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Email Campaigns</title>


    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Geist+Mono:wght@100..900&family=Manrope:wght@200..800&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&family=Raleway:ital,wght@0,100..900;1,100..900&family=Roboto+Mono:ital,wght@0,100..700;1,100..700&family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap" rel="stylesheet">
  <style>
    body {
   font-family: "Poppins", sans-serif;
      background: #fafafa;
      color: #222;
      margin: 0;
      padding: 22px;
    }
    h1 {
      text-align: center;
      margin-bottom: 24px;
      font-style: normal;
      font-weight: 700;

    }
    table {
      width: 100%;
      border-collapse: collapse;
      background: #fff;
      margin: 0 auto 24px auto;
      max-width: 850px;
      box-shadow: 0 0 3px #ddd;
      
    }
    th, td {
      border: 1px solid #eee;
      padding: 10px 8px;
      text-align: left;
      vertical-align: top;
      font-size: 1rem;
      font-family: "Poppins", sans-serif;
    }
    th {
      background: #f4f4f4;
      font-style: italic;
      font-weight: 600;
    }
    .recipients-container {
      max-height: 38px;
      overflow-y: auto;
      font-size: 0.9em;
      background: #f7f7f7;
   
      font-style: normal;
    }
    .status {
      font-weight: bold;
      text-transform: capitalize;
      font-size: 0.95em;
      font-style: normal;
    }
    tr td:first-child {
      font-style: normal;
      font-weight: 500;
    }
    a {
      display: block;
      width: fit-content;
      margin: 16px auto 0 auto;
      text-decoration: none;
      color: #1565c0;
      font-weight: 600;
      padding: 7px 16px;
      border-radius: 4px;
      background: #e3ecf7;
      font-size: 1em;
      font-family: inherit;
    }
    a:hover {
      background: #d0dffc;
    }
    .no-campaigns {
      text-align: center;
      color: #888;
      font-style: italic;
      margin-top: 60px;
    }
  </style>
  
</head>
<body>
  <h1 style="color: #1565c0;">All Email Campaigns</h1>
  <% if (campaign.length === 0) { %>
    <div class="no-campaigns">No campaigns found.</div>
  <% } else { %>
    <table>
      <thead>
        <tr>
          <th>Title</th>
          <th>Message</th>
          <th>Recipients</th>
          <th>Scheduled Time</th>
          <th>Status</th>
        </tr>
      </thead>
      <tbody>
        <% campaign.forEach(c => { %>
        <tr>
          <td><%= c.title %></td>
          <td><%= c.message.slice(0, 60) %>...</td>
          <td>
            <div class="recipients-container"><%= c.recipients.join(", ") %></div>
          </td>
          <td><%= new Date(c.scheduledTime).toLocaleString() %></td>
          <td class="status"><%= c.status %></td>
        </tr>
        <% }) %>
      </tbody>
    </table>
  <% } %>
  <a href="/">Create New Campaign</a>
</body>
</html>
